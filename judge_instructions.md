# T35 判定者への指示（この文をそのまま貼る・凍結 v2 2026-09-19 16:xx）

> v2: ①回答を 4 値（YES／NO／不明／**該当なし**）に＝対象の要素が無い頁を NO と数えない ②渡す画像を「そのルールに要る幅だけ」に（R3 は 1280 のみ・R4〜R10 は 375 のみ・R1・R2 は両方）③R2 は H2 を 1 本ずつ等倍で切った画像（`_R2_1`…`_R2_4`）④自由記述は最大 3 件 ⑤画像は gold と同じ headless Chrome（CDP）から撮影＝`manifest.json` に commit・Chrome の版・時刻・viewport・dsf・各 PNG の sha256 を記録。v1 の失敗＝iframe で測った位置と headless の描画が 375 で 300〜700px ずれ、R8 が 6 頁で空白だった。

対象＝12 頁。頁ごとにフォルダ 1 つ＝画像: `375_first.png`・`1280_first.png`（ファーストビュー）と `<幅>_R<n>.png`（ルール n の該当箇所の等倍切り出し。**無いルールは対象要素が無い頁**）。1 頁＝1 回の依頼（12 回 × モデル 2 つ＝24 応答）。応答は加工せず原文のまま保存する。まず **1 頁 × 2 モデルの pilot**（`top`）で画像の参照ミスが無いことを見てから残り 11 頁。

---

添付した画像だけを見て、次の 10 ルールについてこの頁に違反があるかを、ルールごとに **YES／NO／不明／該当なし** で答え、根拠を 1 行ずつ書いてください。

- 画像の名前の `R1`〜`R10` がルール番号です（`375_R5.png`＝375 幅のルール 5 の箇所）。`R2_1`〜`R2_4` は H2 を 1 本ずつ切り出したものです。
- **そのルールの画像が無い場合は「該当なし」**と答えてください（対象の要素がその頁に無い、という意味です）。
- 画像から判断できないときは「不明」と書いてください（推測で YES／NO にしないでください）。数値が読み取れる場合は数値も書いてください。

- R1: 本文（ヘッダー・ナビ・フッタ・フォームを除く）の文字サイズの最小値が 15px 未満
- R2: 同じ頁の H2（節見出し）に 2 種類以上の大きさが混在している（幅ごとに答える）
- R3: 本文の 1 行の長さが 38em（全角 38 字相当）を超えている（1280 幅の画像）
- R4: 375 幅で h1（頁の題）が 5 行以上
- R5: 375 幅でラジオボタンの 4 択が折り返して縦に伸び、高さが 100px を超えている
- R6: 375 幅で目次（details の summary）の高さが 44px 未満
- R7: 375 幅で目次の項目リンクの高さが 44px 未満
- R8: 375 幅でフッタの地図（サイト内リンク一覧）のリンクの高さが 44px 未満
- R9: 375 幅で Works の小ナビ（点検と修理／業務自動化／…）のリンクの高さが 44px 未満
- R10: 375 幅でパンくずのリンクの高さが 44px 未満

回答の形式:
```
R1: YES／NO／不明／該当なし — 根拠（数値があれば）
R2（375）: … ／ R2（1280）: …
R3: …
…
R10: …
```

最後に、上の 10 ルール以外で「直すべき」と思う箇所があれば **最大 3 件**（場所と理由を 1 行ずつ）。無ければ「なし」。

---

（当方の控え）gold＝`gold_v2_labels.json`（撮影と同じ headless Chrome で測った 7bec67f の DOM 値 × ルール＝違反 38・非違反 58・該当なし 36）。集計＝**該当なしを除いた 96 組**（132 − 36。旧「84」は計算ミス・09-19 夜訂正）で。⚠ 09-19 夜の改訂: 主指標は対照（違反・非違反の両方）が在る R1〜R4 の 60 組＝`results_summary.md`。 TP／FP／FN／TN と不明を数え、判定可能率（YES/NO を出した割合）・判定正解率（YES/NO のうち正しかった割合）・不明率・FP 率・FN 率の 5 指標。自由記述は別枠（3 分類）で主指標に混ぜない。採点はモデル名を伏せてシャッフル・運用者が 5 件を抜き取り。
